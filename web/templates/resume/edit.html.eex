<h2>Edit resume</h2>

<%= render "form.html", changeset: @changeset,
                        resume: @resume,
                        action: resume_path(@conn, :update, @resume),
                        user: @user %>

<%= link "Back", to: resume_path(@conn, :index) %>
